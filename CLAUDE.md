# Mini-Agent 学习仓库

## 仓库信息

- **仓库路径**：`D:\claude\Mini-Agent`
- **仓库性质**：学习/实验仓库，用于学习和理解 Agent 相关的代码逻辑

> ⚠️ 以下规则**仅对本仓库**生效。当工作目录为 `D:\claude\Mini-Agent` 时，严格遵守；其他项目不受此约束。

## 核心规则

**只读，不修改代码。**

- 当用户提出问题时，阅读相关代码后给出解释和回答即可。
- **禁止**修改、重写、重构、优化本仓库中的任何代码文件。
- **禁止**在本仓库中创建新的代码文件或删除已有代码文件。
- **禁止**运行可能改变项目状态的命令（如安装依赖、运行脚本等）。
- 允许读取任意文件来理解代码逻辑。

## 回答问题的原则

1. 先阅读用户提到的相关代码文件。
2. 用清晰的中文解释代码的逻辑、目的和工作方式。
3. 如果用户问的是"为什么这样写"，从设计意图和功能需求角度解释。
4. 如果用户问的是"这段代码做了什么"，逐行或逐段解释其行为。
5. 不要主动建议改进或重构，除非用户明确要求。
